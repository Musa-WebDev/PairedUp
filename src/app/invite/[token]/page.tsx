import { acceptInvitationAction } from '@/actions/workspace'
export default async function InvitePage({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; return <main className="grid min-h-screen place-items-center bg-slate-50 p-6 dark:bg-[#0f172a]"><form action={acceptInvitationAction.bind(null, token)} className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-8 text-center shadow-xl dark:border-slate-700 dark:bg-slate-800"><p className="text-sm font-semibold text-indigo-500">PAIRUP INVITATION</p><h1 className="mt-2 text-2xl font-bold">Join this shared space</h1><p className="mt-3 text-sm text-slate-500">Sign in or create an account first, then accept your invitation.</p><button className="mt-6 h-11 w-full rounded-xl bg-indigo-600 font-semibold text-white">Accept invitation</button></form></main> }