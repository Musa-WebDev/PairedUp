import { ChevronLeft, ChevronRight, Plus } from 'lucide-react'
import { ProtectedShell } from '@/components/auth/ProtectedShell'
const days = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat']
export default async function CalendarPage() { const cells = Array.from({length:35},(_,i)=>i-2); return <ProtectedShell><section className="mx-auto max-w-7xl"><div className="mb-6 flex flex-wrap items-center gap-3"><button className="rounded-lg border bg-card px-4 py-2 text-sm font-semibold">Today</button><button className="rounded-lg p-2 hover:bg-muted"><ChevronLeft className="size-5"/></button><button className="rounded-lg p-2 hover:bg-muted"><ChevronRight className="size-5"/></button><h1 className="ml-2 text-2xl font-bold">July 2026</h1><button className="ml-auto inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="size-4"/>Create</button></div><div className="overflow-hidden rounded-2xl bg-card shadow-sm"><div className="grid grid-cols-7 border-b">{days.map(day=><div key={day} className="p-3 text-center text-xs font-bold uppercase tracking-wide text-muted-foreground">{day}</div>)}</div><div className="grid grid-cols-7">{cells.map((day,i)=><div key={i} className="min-h-28 border-b border-r p-2 last:border-r-0 sm:min-h-36"><span className={`grid size-7 place-items-center rounded-full text-sm ${day===28?'bg-blue-600 font-bold text-white':''} ${day<1||day>31?'text-slate-300':'font-medium'}`}>{day>0&&day<=31?day:''}</span>{day===12&&<p className="mt-2 rounded-md bg-purple-100 px-2 py-1 text-xs font-semibold text-purple-700">Group check-in</p>}{day===18&&<p className="mt-2 rounded-md bg-rose-100 px-2 py-1 text-xs font-semibold text-rose-700">Date night</p>}</div>)}</div></div></section></ProtectedShell> }