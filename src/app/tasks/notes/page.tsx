import { ProtectedShell } from '@/components/auth/ProtectedShell'
import { createNoteAction } from '@/actions/work'
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
export default async function NotesPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:m}=await s.from('workspace_members').select('workspace_id').eq('user_id',user.id).limit(1);const w=m?.[0]?.workspace_id;if(!w)return <ProtectedShell><p>Create a workspace first.</p></ProtectedShell>;const [{data:notes},{data:tasks},{data:projects}]=await Promise.all([s.from('notes').select('id,title,body,task_id,project_id,created_at').eq('workspace_id',w).order('created_at',{ascending:false}),s.from('tasks').select('id,title').eq('workspace_id',w),s.from('projects').select('id,title').eq('workspace_id',w)]);return <ProtectedShell><section className="mx-auto max-w-6xl"><p className="text-sm font-bold text-blue-600">KNOWLEDGE</p><h1 className="mt-1 text-3xl font-bold">Notes</h1><div className="mt-7 grid gap-6 lg:grid-cols-[.75fr_1.25fr]"><form action={createNoteAction} className="rounded-2xl bg-card p-5 shadow-sm"><h2 className="font-bold">New note</h2><input type="hidden" name="workspaceId" value={w}/><input required name="title" placeholder="Note title" className="mt-4 h-11 w-full rounded-xl border bg-transparent px-3"/><textarea required name="body" placeholder="Write your note…" className="mt-3 min-h-40 w-full rounded-xl border bg-transparent p-3"/><label className="mt-3 block text-xs font-semibold">Attach to task (optional)<select name="taskId" className="mt-1 h-10 w-full rounded-lg border bg-transparent px-2"><option value="">No task</option>{tasks?.map(t=><option key={t.id} value={t.id}>{t.title}</option>)}</select></label><label className="mt-3 block text-xs font-semibold">Attach to project (optional)<select name="projectId" className="mt-1 h-10 w-full rounded-lg border bg-transparent px-2"><option value="">No project</option>{projects?.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select></label><button className="mt-4 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">Save note</button></form><div className="grid gap-4 sm:grid-cols-2">{notes?.map(n=><article key={n.id} className="rounded-2xl bg-card p-5 shadow-sm"><h2 className="font-bold">{n.title}</h2><p className="mt-2 whitespace-pre-wrap text-sm text-muted-foreground">{n.body}</p>{(n.task_id||n.project_id)&&<p className="mt-4 rounded-full bg-purple-100 px-3 py-1 text-xs font-semibold text-purple-700">Attached to {n.task_id?'task':'project'}</p>}</article>)}{!notes?.length&&<p className="text-muted-foreground">No notes yet.</p>}</div></div></section></ProtectedShell>}