import { ProtectedShell } from '@/components/auth/ProtectedShell'
import { createProjectAction, createTaskAction } from '@/actions/work'
import { TaskCard } from '@/components/tasks/TaskCard'
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
export default async function ProjectsPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:m}=await s.from('workspace_members').select('workspace_id').eq('user_id',user.id).limit(1);const w=m?.[0]?.workspace_id;if(!w)return <ProtectedShell><p>Create a workspace first.</p></ProtectedShell>;const [{data:projects},{data:tasks}]=await Promise.all([s.from('projects').select('id,title,description,status,due_date').eq('workspace_id',w).order('created_at',{ascending:false}),s.from('tasks').select('id,title,status,project_id,due_date,due_time').eq('workspace_id',w).order('created_at',{ascending:false})]);return <ProtectedShell><section className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Projects & tasks</h1><div className="mt-6 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form action={createProjectAction} className="rounded-2xl bg-card p-5 shadow-sm"><input type="hidden" name="workspaceId" value={w}/><h2 className="font-bold">New project</h2><input required name="title" placeholder="Project name" className="mt-4 h-11 w-full rounded-xl border bg-transparent px-3"/><textarea name="description" placeholder="Description" className="mt-3 min-h-24 w-full rounded-xl border bg-transparent p-3"/><input name="dueDate" type="date" className="mt-3 h-11 w-full rounded-xl border bg-transparent px-3"/><button className="mt-3 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">Create project</button></form><div className="space-y-4">{projects?.map(p=><article key={p.id} className="rounded-2xl bg-card p-5 shadow-sm"><h2 className="font-bold">{p.title}</h2><p className="mt-1 text-sm text-muted-foreground">{p.description||'No description'} {p.due_date&&`· Due ${p.due_date}`}</p><form action={createTaskAction} className="mt-4 grid gap-2 sm:grid-cols-[1fr_auto_auto_auto]"><input type="hidden" name="workspaceId" value={w}/><input type="hidden" name="projectId" value={p.id}/><input required name="title" placeholder="Add a task" className="h-10 rounded-xl border bg-transparent px-3"/><input name="dueDate" type="date" max={p.due_date??undefined} className="h-10 rounded-xl border bg-transparent px-2 text-sm"/><input name="dueTime" type="time" className="h-10 rounded-xl border bg-transparent px-2 text-sm"/><button className="rounded-xl bg-purple-600 px-3 text-sm font-semibold text-white">Add</button></form><ul className="mt-4 space-y-3">{tasks?.filter(t=>t.project_id===p.id).map(t=><TaskCard key={t.id} task={t} projectDueDate={p.due_date}/>)}</ul></article>)}{!projects?.length&&<p className="rounded-2xl bg-card p-6 text-muted-foreground">No projects yet.</p>}</div></div></section></ProtectedShell>}