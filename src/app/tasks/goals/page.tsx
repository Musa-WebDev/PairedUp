import { ProtectedShell } from '@/components/auth/ProtectedShell'
import { NewGoalForm } from '@/components/forms/NewWorkForms'
import { GoalCard } from '@/components/goals/GoalCard'
import { createClient } from '@/lib/supabase/server'
import { getWorkspaceContext } from '@/lib/workspace-context'
import { redirect } from 'next/navigation'

export default async function GoalsPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { activeWorkspace } = await getWorkspaceContext(); if (!activeWorkspace) return <ProtectedShell><p>Create a workspace first.</p></ProtectedShell>; const [{ data: goals }, { data: tasks }] = await Promise.all([supabase.from('goals').select('id,title,description,status,target_date,term,postponement_count').eq('workspace_id', activeWorkspace.id).neq('status', 'achieved').order('target_date'), supabase.from('tasks').select('id,title,status,due_date,due_time,goal_id').eq('workspace_id', activeWorkspace.id).neq('status', 'completed').order('created_at')]); return <ProtectedShell><section className="mx-auto max-w-6xl"><p className="text-sm font-bold text-blue-600">{activeWorkspace.name}</p><div className="mt-1 flex flex-wrap items-center justify-between gap-4"><h1 className="text-3xl font-bold">Goals</h1><NewGoalForm workspaceId={activeWorkspace.id}/></div><div className="mt-7 space-y-4">{goals?.map((goal) => <GoalCard key={goal.id} goal={goal} workspaceId={activeWorkspace.id} tasks={tasks?.filter((task) => task.goal_id === goal.id) ?? []}/>)}{!goals?.length && <p className="rounded-2xl bg-card p-6 text-muted-foreground">No goals yet.</p>}</div></section></ProtectedShell> }