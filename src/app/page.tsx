import Link from 'next/link'
import { CalendarDays, CirclePlus, FileText, FolderKanban, Target } from 'lucide-react'
import { ProtectedShell } from '@/components/auth/ProtectedShell'

const cards = [{ title: 'Open tasks', value: '12', icon: FolderKanban, color: 'text-blue-600 bg-blue-50' }, { title: 'Goals in progress', value: '5', icon: Target, color: 'text-purple-600 bg-purple-50' }, { title: 'Upcoming plans', value: '3', icon: CalendarDays, color: 'text-rose-600 bg-rose-50' }]
export default async function DashboardPage() { return <ProtectedShell><div className="mx-auto max-w-7xl space-y-8"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-bold text-blue-600">OVERVIEW</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Your shared dashboard</h1><p className="mt-2 text-muted-foreground">Everything your group is planning, building, and remembering.</p></div><Link href="/tasks/projects" className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white shadow-sm hover:bg-blue-500"><CirclePlus className="size-4" />Create new</Link></section><section className="grid gap-4 md:grid-cols-3">{cards.map(({ title, value, icon: Icon, color }) => <article key={title} className="rounded-2xl bg-card p-5 shadow-sm transition-shadow hover:shadow-md"><span className={`grid size-10 place-items-center rounded-xl ${color}`}><Icon className="size-5" /></span><p className="mt-5 text-3xl font-bold">{value}</p><p className="mt-1 text-sm text-muted-foreground">{title}</p></article>)}</section><section className="grid gap-6 lg:grid-cols-2"><Panel title="Recent activity" items={['New project: Plan our next trip', 'Goal updated: Read 12 books', 'Note added: Friday movie ideas']} /><Panel title="Coming up" items={['Team check-in · Today', 'Date night planning · Friday', 'Monthly goals review · Sunday']} /></section></div></ProtectedShell> }
function Panel({ title, items }: { title: string; items: string[] }) { return <section className="rounded-2xl bg-card p-6 shadow-sm"><div className="mb-5 flex items-center justify-between"><h2 className="font-bold">{title}</h2><FileText className="size-5 text-purple-600" /></div><ul className="space-y-3">{items.map(item => <li key={item} className="rounded-xl bg-slate-50 px-4 py-3 text-sm text-slate-700 dark:bg-slate-700/50 dark:text-slate-200">{item}</li>)}</ul></section> }